<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" />


    <style name="CustomDatePickerDialog" parent="ThemeOverlay.AppCompat.Dialog">
        <!-- Customize the attributes you want -->
        <item name="android:datePickerStyle">@style/CustomDatePickerStyle</item>
    </style>

    <style name="CustomDatePickerStyle" parent="@android:style/Widget.Material.DatePicker">
        <!-- Customize the DatePicker widget attributes -->
        <!-- For example, you can set a custom background, text color, etc. -->
    </style>
</resources>